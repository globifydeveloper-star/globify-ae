"use client";

// Next.js App Router automatically handles scroll restoration on route changes.
// This component is kept as a no-op to avoid breaking existing imports.
export const ScrollToTop = () => null;
